# App Identity Test

Source: `src/lib/utils/app-identity.test.mjs`.

- `request()` fixture resolves runtime identity.
- Case verifies deferred name and document-title updates after the request resolves.

Target: [`app-identity.ts`](app-identity.ts.md).
