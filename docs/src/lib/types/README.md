# Frontend Types

Serialized contracts shared across frontend feature boundaries.

- [`app.ts`](app.ts.md) — application identity.
- [`workspace.ts`](workspace.ts.md) — project graph, snapshots, patches, reads, saves, watcher errors.

Field names mirror Rust `camelCase` serialization.
