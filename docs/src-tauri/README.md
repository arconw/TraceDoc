# Tauri Backend

Only runtime Rust code is mirrored here.

- [`src`](src/README.md) — commands, serialized models, filesystem/index/watcher services, application bootstrap.

Cargo/Tauri configuration and generated targets are intentionally excluded.
